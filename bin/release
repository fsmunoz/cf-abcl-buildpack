#!/bin/sh
# bin/release <build-dir>

cat <<EOF
---
config_vars:
  PATH: .abcl:.maven/bin:.jdk/bin:/usr/local/bin:/usr/bin:/bin
  JAVA_HOME: .jdk
  JVM_OPTS: -server -Xrs
default_process_types:
  web: abcl --load .init2.lisp; sleep 1h
EOF
